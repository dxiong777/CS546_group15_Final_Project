<a class="get-home" href="/shop">To Home Page</a>
<main id="ab">
    <h1>{{title}}</h1>

    <ul>
        {{#each allShop}}
        <li>
             {{!-- {{this.name}} --}}
            <a class="person-item" href="/shopId/{{this._id}}">{{this.name}} </a>
        </li>
        <br>
        {{/each}}
    </ul>

</main>